# Contributing

1. Open a branch from `main`.
2. Keep skills compatible with the Agent Skills frontmatter (`name`, `description`).
3. Do not add secrets.
4. Run `python -m agent_skills.cli validate` and `pytest` before PR.
5. Reference files under `references/repositories/` are knowledge only — do not turn them into skills.
6. Prefer small, reviewable PRs.
